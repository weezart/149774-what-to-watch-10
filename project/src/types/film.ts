export type Film = {
  id: string;
  name: string;
  image: string;
};

export type FilmInfo = {
  id: string;
  poster: string;
  imageBg: string;
  name: string;
  genre: string,
  release: number,
};
